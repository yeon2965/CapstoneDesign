<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imaginarywings.capstonedesign.remo">

    <!-- intent로 불러올 기본 camera feature 설정 -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- Needed only if your app targets Android 5.0 (API level 21) or higher. 위치 권한 요청청-->    <uses-permission android:name = "android.permission.ACCESS_FINE_LOCATION" />
    <!-- <uses-feature android:name = "android.hardware.location.gps" /> -->

    <!-- 기존 카메라를 사용하려면 이 퍼미션이 없어도 됨 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-permission android:name="android.hardware.camera.autofocus" />

    <!-- 사진을 외부 저장소에 저장할수 있도록 외부 저장소 write permission 설정-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"> <!--타이틀바 없앰-->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".PeopleActivity"
            android:label = "People">

        </activity>

        <activity android:name=".CameraActivity"
            android:label = "Camera">

        </activity>

    </application>

</manifest>